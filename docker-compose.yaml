services:
  database:
    image: mongodb/mongodb-community-server:latest
    ports:
      - "27017:27017"
    volumes:
      - db_data:/data/db
  app:
    build: .
    ports:
      - "8000:8000"
    expose:
      - "8000"
    depends_on:
      - database
    environment:
      DB_URL: "mongodb://database:27017/"
      SECRET: "123456"
volumes:
  db_data:
